# Core dependencies for ocr_to_markdown.py (Mistral OCR -> Markdown pipeline)
mistralai>=2.5.0
python-dotenv>=1.0.0
pypdf>=6.0.0

# Note: the scripts under legacy/ additionally use: anthropic, pdf2image, Pillow
# (and the poppler binary). They are NOT required by ocr_to_markdown.py.
